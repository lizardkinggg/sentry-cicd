version: '3'
services:
  {{ registry_admin }}:
    image: {{ registry_url }}/{{ registry_admin }}/{{ project_name }}:{{ commit_sha }}
    ports:
      - "5000:5000"
    environment:
      - dsn=${dsn}
    restart: always
